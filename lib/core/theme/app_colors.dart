import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand color (Sophisticated deep green)
  static const Color primary = Color(0xFF1B4332);
  static const Color primaryLight = Color(0xFF2D6A4F);
  static const Color primaryDark = Color(0xFF081C15);

  // Accents (Rich gold/mustard for premium feel)
  static const Color accent = Color(0xFFC7923E);
  static const Color highlightAccent = Color(0xFFE8B65A);

  // Backgrounds (Clean, slightly warm for approachability)
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardSurface = Color(0xFFF4F6F4);

  // Text
  static const Color textPrimary = Color(0xFF1E1E1E);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFFA0A0A0);

  // System & Borders
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderLight = Color(0xFFF0F0F0);
  static const Color divider = Color(0xFFEEEEEE);

  // Semantic
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFED6C02);
  static const Color info = Color(0xFF0288D1);

  // Special effects
  static const Color glassmorphismOverlay = Color(0x33FFFFFF);
  static const Color shadowColor = Color(0x1A000000);
}
