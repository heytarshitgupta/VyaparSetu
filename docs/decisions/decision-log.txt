============================================================
ENGINEERING DECISION LOG
============================================================

This log records permanent architectural, design, and structural decisions made for VyaparSetu.


2026-09-02
------------------------------------------------------------
- Buyer Module Isolation: The Buyer module under lib/buyer_section/ is owned by another teammate and is strictly treated as READ-ONLY for Producer development to prevent regressions.
- Standardized Terminology: The term Producer is adopted across code, routes, database schemas, and documentation (replacing "Seller" for all new development).
- Branch Strategy: Producer development occurs on the dedicated producer-development branch.
- Security-First Development: Security, authorization, and Row Level Security (RLS) are development-time requirements built incrementally at each stage, not added as a post-development polish item.
- Responsive UI Constraint: All future screens must avoid fixed-size overflows and support diverse screen sizes and text scaling via flexible Flutter layouts.
- Low-Literacy Friendly UX: UX is engineered specifically for grassroots micro-producers with clear icons, large touch targets, simplified terminology, and step-by-step guidance.
- Decoupled Language Preferences: The application architecture decouples App UI Language from Voice Guidance Spoken Language (e.g. English UI with Punjabi audio narration).
- Optional Voice Guidance: Audio guidance is strictly optional, user-controlled, and can be disabled at any time.
- Auditable SQL History: All SQL queries, migrations, and table definitions executed against Supabase must be stored as versioned files in supabase/sql/ inside the Git repository.
- Live Documentation: Documentation will use .txt files for easy reading and management. Executable database scripts will continue to use .sql.
- Preserve Nested Repo Structure: The existing nested /VyaparSetu/VyaparSetu folder structure is preserved to avoid disturbing working directory paths and tooling.
- Single Configuration Method: Project configuration is standardized on a single root .env file loaded via flutter_dotenv, with generic placeholders in .env.example.
- Database Foundation Schema & Privileges:
  * Migration 001_database_foundation.sql executed in Supabase.
  * Private-by-Default RLS enabled on profiles, producer_profiles, and app_sessions.
  * Explicit column-level privilege whitelist: direct modifications to profiles.role and producer_profiles.verification_status are disbarred for authenticated clients.
  * Concurrency enforcement: Partial unique index on app_sessions(user_id) WHERE (is_active = TRUE).
  * Secure Session RPC: register_app_session() derives user identity strictly from auth.uid(), sets search_path = '', validates 64-hex token hash, and deactivates prior sessions atomically.
- Producer Authentication Milestone (Step 3 Finalized):
  * Shared AuthService implemented under lib/core/auth/.
  * Shared RoleSelectionScreen created as initial gateway ('/') cleanly routing Buyer ('/buyer_auth') and Producer ('/producer_login') without touching lib/buyer_section/.
  * ProducerLoginScreen, ProducerSignupScreen, and ProducerAuthService implemented.
  * Strict Role Policy: register_producer_profile RPC and ProducerAuthService validate Producer role and reject existing Buyer and Admin accounts.
  * Unauthorized Session Termination: If a user authenticates with Supabase Auth but fails Producer portal authorization, the client immediately terminates (signs out) the active session.
  * Migration 004_fix_producer_rpc_sql_expressions.sql executed and live-verified in Supabase.
- Producer Onboarding Foundation & Data Model (Step 4A & 4B0.3 Finalized):
  * Multi-step onboarding shell (5 steps) and provider implemented.
  * Post-login/signup routing dynamically checks onboarding_status ('not_started'/'in_progress' -> '/producer_onboarding', 'completed' -> '/producer_home').
  * Migration 005_expand_producer_onboarding_schema.sql executed in Supabase: explicitly revokes client write privileges on onboarding_status (granted in 001); restricts client writes to declared inputs (business_name, craft_category, bio, state, district, city, pincode, address, gst_registered, gstin); keeps all identity verification artifacts (pan_last4, pan_hash, aadhaar_last4, aadhaar_verification_reference) and verification statuses strictly backend-only. Prohibits storage of raw Aadhaar.
- Producer Basic Details Form Milestone (Step 4B1):
  * Step 1 (Basic Details) UI implemented (BasicDetailsStep) with prefilling from public.profiles and auth.users.
  * Source of truth: Supabase Auth is authoritative for email (read-only) and verified phone (read-only). If no auth phone exists, public.profiles.phone is treated strictly as an editable contact phone (10-digit Indian mobile validation) rather than verified authentication.
  * Full Name (2–120 chars) and contact phone are persisted to public.profiles before step progression.
- Producer Business and Location Onboarding Milestone (Step 4C & 4D Finalized):
  * Step 2 (Craft & Business Details) implemented: Business/Workshop Name (2–120 chars), 10 single-choice craft category chips with icons, dynamic 'Other' custom category entry (2–60 chars), and optional short bio (max 500 chars with live counter).
  * Step 3 (Location Details) implemented: State dropdown with 36 Indian states & UTs, District (2–100 chars), City/Village (2–100 chars), strict Pincode matching database constraint chk_producer_pincode (^[1-9][0-9]{5}$), and multi-line Address (5–300 chars).
  * Column Privilege Guard (42501 Resolution): Confirmed client update payloads must omit 'updated_at' because column UPDATE privilege is deliberately restricted to user-editable fields in Migration 001. Timestamp updates are handled strictly by database triggers trg_profiles_updated_at and trg_producer_profiles_updated_at.
- Identity & Compliance UI & Simulated PAN Verification (Step 4E1 & 4E2):
  * Step 4 UI implemented (IdentityComplianceStep) with clear subtitle, subtle prototype simulation disclosure, reusable VerificationStatusBadge, active simulated PAN card, and unavailable "Coming Soon" cards for upcoming Aadhaar, GST, and Address workflows.
  * Raw PAN Safety & Zero Persistence: Plaintext 10-character PAN is strictly transient in widget memory (TextEditingController). It is never stored in ProducerOnboardingProvider, never saved to SharedPreferences, never logged, and never persisted to Supabase tables.
  * Migration 006_pan_verification_prototype.sql created (unexecuted): Defines public.verify_producer_pan_prototype() with SECURITY DEFINER and search_path = ''. Uses auth.uid() exclusively, checks producer role, evaluates deterministic demo simulation rule (PAN starting with 'FAIL' or 4th char 'X' simulates rejection; all other valid PANs simulate success), extracts 4-character pan_last4, computes 64-hex SHA-256 pan_hash, updates producer_profiles without client write privileges, and returns safe masked identifier ('******234F'). Zero raw PAN returned.
- Resumable Producer Onboarding Architecture (Step 4E2.1):
  * Problem: Reopening the app, refreshing the browser, losing network connectivity, or signing out previously reset the producer to Step 1, ignoring earlier persisted draft records.
  * Server-Authoritative Progress: Stored public.producer_profiles.onboarding_step (SMALLINT, CHECK 1 to 5) as the single source of truth. Local persistence mechanisms (SharedPreferences, localStorage, static state) are rejected as authoritative sources to ensure cross-device consistency and prevent client-side bypass.
  * Progression & Anti-Skipping: Direct client UPDATE on onboarding_step is omitted. Progression is governed strictly by public.advance_producer_onboarding_step(expected_current_step, next_step) RPC (SECURITY DEFINER, search_path = '', caller auth.uid() check, producer role check). Progression allows advancement of at most 1 step beyond server's known progress.
  * Monotonic & Idempotent: If the user presses "Back" in the UI to review or re-edit prior steps, the server's persisted onboarding_step does NOT regress. The RPC treats calls where server_step >= next_step as idempotent no-ops.
  * Save-First Advancement: The server progress is updated only after the corresponding form payload save completes successfully. If save fails, progress does not advance.
  * Initial Load Restoration: On screen load, ProducerOnboardingScreen fetches producer_profiles, extracts onboarding_step, and initializes directly to the saved step with draft data prefilled.
- Security and Correctness Hardening of Migrations 006 and 007 (Step 4E2.3):
  * Migration 006 Verified Identity Protection: If caller already has pan_verification_status = 'verified', a matching PAN returns 'already_verified' (idempotent success); a differing or failing PAN returns 'already_verified_conflict' and NEVER destroys, erases, or alters verified artifacts (pan_last4, pan_hash, status).
  * Fingerprint Documentation: Clarified that sha256(pan || ':' || uid) is a prototype pseudonymous integrity fingerprint, not encryption, and production requires server-secret HMAC or vault tokenization.
  * Migration 007 Expected Step & Stale State Guard: Uses expected_current_step to enforce server_step = expected_current_step and next_step = expected_current_step + 1, guarding against stale client state and race conditions.
  * Server-Side Prerequisite Enforcement: RPC verifies saved database records (1->2 requires profiles.full_name; 2->3 requires producer_profiles business_name & craft_category; 3->4 requires valid location fields and pincode). Flutter validation is UX; RPC is the security boundary.
  * Step 4 -> 5 Blocked: Generic progression RPC rejects advancing past Step 4 ('identity_compliance_incomplete') because compliance completion rules (PAN, Aadhaar, GST, Address) are still evolving.
  * Backfill Rule Correction: Removed premature pan_verification_status = 'verified' -> 5 inference. Step 4 encompasses full identity and compliance. Added city and string length checks to Location backfill.
  * Concurrency Row Lock: Used SELECT ... FOR UPDATE on producer_profiles to serialize concurrent requests.
- Step 4E2.4 Runtime Verification & Execution Milestone:
  * Migrations 006 and 007 confirmed executed in Supabase.
  * Verified server-authoritative progress: public.producer_profiles.onboarding_step (SMALLINT, CHECK 1 to 5) accurately backfilled existing test artisan to Step 4.
  * Direct client mutations to restricted columns (onboarding_step, onboarding_status, pan_last4, pan_hash, pan_verification_status) verified blocked by PostgreSQL column permissions (42501).
  * Step 4 -> 5 advancement verified blocked live by advance_producer_onboarding_step ('identity_compliance_incomplete').
  * Resumable onboarding verified: browser reloads restore directly to Step 4; Back button navigates locally without regressing server step.
- Step 4E2.6 Live PAN Flow Verification & Migration 008 Execution:
  * Migration 008 executed in Supabase replacing verify_producer_pan_prototype with pg_catalog.btrim.
  * Live runtime verification confirmed: demo PAN ABCDE1234F verified successfully.
  * Database verified: pan_last4 = '234F', pan_hash is 64 hex characters, pan_verification_status = 'verified'. Plaintext PAN ABCDE1234F is not stored.
  * Same-PAN idempotency confirmed: returns 'already_verified' without state mutation.
  * Different-PAN conflict confirmed: returns 'already_verified_conflict' and protects verified artifacts from modification.
- Step 4E3 Identity & Compliance UI Structure Completion:
  * Ordered cards: 1. PAN Verification, 2. Aadhaar Verification, 3. GST Registration.
  * Aadhaar Verification: Replaced generic 'Coming Soon' placeholder with proper card; deferred real Aadhaar verification and zero Aadhaar collection in prototype; informational popup on tap.
  * GST Registration: Replaced placeholder with interactive Yes/No selector and 15-character GSTIN input with regex validation; deferred verification simulation; preserved 'Not Verified' status.
  * Removed Address Verification card from Identity & Compliance (Step 4) MVP UI while keeping mandatory physical location fields in Step 3 intact.
  * Validated with 24/24 passing tests, zero analyze errors, and zero Buyer section changes.







