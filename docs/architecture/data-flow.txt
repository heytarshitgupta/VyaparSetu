============================================================
APPLICATION DATA FLOW
============================================================

STATUS
------------------------------------------------------------
IMPLEMENTED (Authentication Flow Milestone).


1. PRODUCER SIGNUP AND REGISTRATION FLOW (IMPLEMENTED)
------------------------------------------------------------

[Producer App Open]
       |
       v
[Producer Signup Screen: Email + Password + Full Name]
       |
       v
[Supabase Auth (auth.signUp)] ---> Creates auth.users & returns session
       |
       v
[RPC: register_producer_profile] ---> [Executes with auth.uid() & search_path = '']
                                             |
                                             +---> Validates full_name (2-120 chars)
                                             +---> Rejects if existing role is 'buyer' or 'admin'
                                             +---> Inserts public.profiles (role = 'producer')
                                             +---> Inserts public.producer_profiles (onboarding_status = 'not_started')
       |
       v
[Validation: ProducerAuthService.validateProducerAccess]
       |
       v
[Producer Account Created & Verified]


2. PRODUCER LOGIN AND ROLE VALIDATION FLOW (IMPLEMENTED)
------------------------------------------------------------

[Producer Login Screen: Email + Password]
       |
       v
[Supabase Auth (signInWithPassword)] ---> Returns JWT
       |
       v
[Validation: ProducerAuthService.validateProducerAccess]
       |
       +---> If role == 'producer' & producer_profiles exists:
       |            |
       |            v
       |     [Authentication Success]
       |
       +---> If role == 'buyer':
       |            |
       |            v
       |     [Auto Sign Out] ---> [Display: "Registered as Buyer. Access restricted."]
       |
       +---> If role == 'admin':
                    |
                    v
             [Auto Sign Out] ---> [Display: "Admin accounts cannot access Producer portal."]


3. FUTURE ONBOARDING & DASHBOARD FLOWS (PLANNED)
------------------------------------------------------------
- Step 4: Full single-active-session concurrency enforcement (app_sessions)
- Step 5: Multi-step Producer Onboarding wizard (craft, bio, location, documents)
- Step 6: Producer Home Dashboard
