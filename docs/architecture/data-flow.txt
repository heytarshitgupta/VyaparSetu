============================================================
APPLICATION DATA FLOW
============================================================

STATUS
------------------------------------------------------------
IMPLEMENTED AND LIVE VERIFIED:
- Step 3: Producer Authentication Milestone
- Step 4A: Producer Onboarding Foundation & Routing


1. SHARED APP STARTUP FLOW (IMPLEMENTED)
------------------------------------------------------------

[App Launch (MaterialApp initialRoute: '/')]
       |
       v
[RoleSelectionScreen]
       |
       +---> "I Want to Buy Products" ───► [Buyer AuthScreen (/buyer_auth)]
       |                                          │ (Teammate's Buyer flow preserved 100% untouched)
       |
       +---> "I Make & Sell Products" ───► [ProducerLoginScreen (/producer_login)]


2. PRODUCER SIGNUP AND REGISTRATION FLOW (IMPLEMENTED)
------------------------------------------------------------

[Producer Signup Screen: Email + Password + Full Name]
       |
       v
[Supabase Auth (auth.signUp)] ---> Creates auth.users & returns session
       |
       v
[RPC: register_producer_profile] ---> [Executes with auth.uid() & search_path = '']
                                             |
                                             +---> Validates full_name (2-120 chars)
                                             +---> Rejects if existing role is 'buyer' or 'admin'
                                             +---> Inserts public.profiles (role = 'producer')
                                             +---> Inserts public.producer_profiles (onboarding_status = 'not_started', verification_status = 'unverified')
       |
       v
[Validation: ProducerAuthService.validateProducerAccess]
       |
       +---> If onboarding_status == 'completed':
       |            |
       |            v
       |     [Navigate: ProducerPlaceholderScreen (/producer_home)]
       |
       +---> If onboarding_status == 'not_started' / 'in_progress':
                    |
                    v
             [Navigate: ProducerOnboardingScreen (/producer_onboarding)]


3. PRODUCER LOGIN AND ROLE VALIDATION FLOW (IMPLEMENTED)
------------------------------------------------------------

[Producer Login Screen: Email + Password]
       |
       v
[Supabase Auth (signInWithPassword)] ---> Authenticates & issues JWT
       |
       v
[Validation: ProducerAuthService.validateProducerAccess]
       |
       +---> If role == 'producer' & producer_profiles exists:
       |            |
       |            +---> If onboarding_status == 'completed':
       |            |            |
       |            |            v
       |            |     [Navigate: ProducerPlaceholderScreen (/producer_home)]
       |            |
       |            +---> If onboarding_status == 'not_started' / 'in_progress':
       |                         |
       |                         v
       |                  [Navigate: ProducerOnboardingScreen (/producer_onboarding)]
       |
       +---> If role == 'buyer':
       |            |
       |            v
       |     [Auto Sign Out] ---> [Display: "Registered as Buyer. Access restricted."]
       |
       +---> If role == 'admin':
                    |
                    v
             [Auto Sign Out] ---> [Display: "Admin accounts cannot access Producer portal."]


4. PRODUCER ONBOARDING DATA FLOW (STEPS 4B1, 4C, 4D, & 4E1 IMPLEMENTED)
------------------------------------------------------------
[Step 1: Basic Details Form]
       |
       +---> Full Name (prefilled from profiles.full_name / user_metadata)
       +---> Email (read-only from Supabase Auth user.email)
       +---> Phone (read-only if auth phone; editable contact phone if no auth phone)
       |
       v (User taps Continue)
[Validation: 2-120 chars name, 10-digit Indian mobile]
       |
       v
[ProducerAuthService.updateBasicProfile] ---> [UPDATE public.profiles (full_name, phone)]
       |
       +---> If Success:
       |     * RPC: public.advance_producer_onboarding_step(expected: 1, next: 2)
       |     * Sets producer_profiles.onboarding_step = 2
       |     * [Advance to Step 2: Craft & Business]
       |
       +---> If Failure: [Stay on Step 1, show user-friendly error]
       |
       v
[Step 2: Business / Craft Details Form]
       |
       +---> Business / Workshop Name (prefilled from producer_profiles.business_name)
       +---> Craft Category (ChoiceChips; custom input if 'Other'; prefilled from producer_profiles.craft_category)
       +---> Short Description (prefilled from producer_profiles.bio; optional, max 500 chars)
       |
       v (User taps Continue)
[Validation: 2-120 chars business name, category selected, 2-60 chars custom category if 'Other', max 500 chars bio]
       |
       v
[ProducerAuthService.updateBusinessProfile] ---> [UPDATE public.producer_profiles (business_name, craft_category, bio)]
       |
       +---> If Success:
       |     * RPC: public.advance_producer_onboarding_step(expected: 2, next: 3)
       |     * Sets producer_profiles.onboarding_step = 3
       |     * [Advance to Step 3: Location Details]
       |
       +---> If Failure: [Stay on Step 2, show user-friendly error]
       |
       v
[Step 3: Location Details Form]
       |
       +---> State (Dropdown; standard 36 Indian states & UTs; prefilled from producer_profiles.state)
       +---> District (prefilled from producer_profiles.district)
       +---> City / Village (prefilled from producer_profiles.city)
       +---> Pincode (prefilled from producer_profiles.pincode)
       +---> Workshop Address (prefilled from producer_profiles.address)
       |
       v (User taps Continue)
[Validation: State selected, 2-100 chars district, 2-100 chars city, ^[1-9][0-9]{5}$ pincode, 5-300 chars address]
       |
       v
[ProducerAuthService.updateLocationProfile] ---> [UPDATE public.producer_profiles (state, district, city, pincode, address)]
       |
       +---> If Success:
       |     * RPC: public.advance_producer_onboarding_step(expected: 3, next: 4)
       |     * Sets producer_profiles.onboarding_step = 4
       |     * [Advance to Step 4: Identity & Compliance]
       |
       +---> If Failure: [Stay on Step 3, show user-friendly error]
       |
       v
[Step 4: Identity & Compliance Form (Step 4E1 & 4E2)]
       |
       +---> PAN Number (transient controller; uppercase; regex ^[A-Z]{5}[0-9]{4}[A-Z]$)
       +---> Name as per PAN (prefilled from fullName; editable; 2-120 chars)
       +---> Date of Birth (date picker; historical date only; transient)
       +---> Aadhaar / GST / Address (Coming Soon / Not Verified cards)
       |
       v (User taps 'Verify PAN')
[Validation: 10-char PAN regex, 2-120 chars name, valid historical DOB]
       |
       v
[ProducerVerificationService.verifyPan]
       |
       v (RPC invocation)
[Supabase RPC: public.verify_producer_pan_prototype (SECURITY DEFINER)]
       |
       +---> Authenticates caller via auth.uid()
       +---> Checks caller role = 'producer' in public.profiles
       +---> Evaluates demo simulation rule:
       |     * PAN starts with 'FAIL' or 4th char 'X' -> REJECTED:
       |       - UPDATE producer_profiles: pan_last4 = NULL, pan_hash = NULL, pan_verification_status = 'rejected'
       |       - Returns: {success: false, status: 'rejected', message: '...'}
       |     * Standard valid PAN -> VERIFIED:
       |       - Computes pan_last4 = RIGHT(pan, 4), pan_hash = SHA-256 hex
       |       - UPDATE producer_profiles: pan_last4 = v_last4, pan_hash = v_hash, pan_verification_status = 'verified'
       |       - Returns: {success: true, status: 'verified', pan_last4: '234F', masked_pan: '******234F'}
       |
       v
[Flutter Client UI Result Handler]
       |
       +---> If Verified: [Badge = Verified, Displays "PAN Verified", Shows masked_pan '******234F', clears raw PAN from memory]
       +---> If Rejected: [Badge = Could Not Verify, Shows friendly rejection message, Button = 'Try Again', form remains editable]
       +---> If Exception: [Sanitizes database/network error -> "Unable to complete verification at this time. Please try again."]
       |
       v (User taps Continue)
[Advance to Step 5: Review & Submit Placeholder]

Note on Timestamps: Client update payloads do NOT include 'updated_at'.
Timestamps on profiles and producer_profiles are automatically set by
database triggers (trg_profiles_updated_at, trg_producer_profiles_updated_at)
under SECURITY DEFINER to maintain column-level privilege boundaries (42501).


5. FUTURE ONBOARDING & DASHBOARD FLOWS (PLANNED)
------------------------------------------------------------
- Step 4E3+: Aadhaar / GST / Address verification workflows
- Step 4F: Step 5 (Review & Submit; trusted transition of onboarding_status -> 'completed')
- Step 5: Single-active-session concurrency enforcement (app_sessions)
- Step 6: Producer Home Dashboard & AI Studio

